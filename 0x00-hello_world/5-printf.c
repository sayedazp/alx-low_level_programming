#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char str1[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str1);
	return (0);
}
